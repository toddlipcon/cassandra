# remove contents of data directories
rm -rf /mnt/d1/data/*
rm -rf /mnt/d2/data/*
rm -rf /mnt/d3/data/*

# remove all the log files
rm -rf /mnt/d2/logs/*

# remove all the commit logs
rm -rf /mnt/d4/commitlog/*
